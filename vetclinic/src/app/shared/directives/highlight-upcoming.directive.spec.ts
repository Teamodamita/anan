import { HighlightUpcomingDirective } from './highlight-upcoming.directive';

describe('HighlightUpcomingDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightUpcomingDirective();
    expect(directive).toBeTruthy();
  });
});
