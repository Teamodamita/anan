import { StatusBadgeDirective } from './status-badge.directive';

describe('StatusBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusBadgeDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
